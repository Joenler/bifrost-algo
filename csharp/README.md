# bifrost-algo/csharp

Placeholder — Phase 08 lands the `Bifrost.Sdk` NuGet package, the `IExecutionStrategy`
interface, the TWAP baseline in `examples/Twap/`, the Dockerfile, and the local
dashboard here.

Until Phase 08 ships, this directory holds only this README.
