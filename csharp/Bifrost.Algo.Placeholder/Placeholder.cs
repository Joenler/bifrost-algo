namespace Bifrost.Algo.Placeholder;

// Smoke class: compiled against Bifrost.Contracts.Strategy from the local NuGet source.
// Phase 08 replaces this with the real Bifrost.Algo.Sdk surface.
public static class Smoke
{
    public static string Name => nameof(Smoke);
}
