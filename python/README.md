# bifrost-algo/python

Placeholder — Phase 09 lands the `bifrost_sdk` pip package (uv-managed), the
`IExecutionStrategy` asyncio protocol, the TWAP baseline in `examples/twap/`, the
Dockerfile, and the local dashboard here.

Until Phase 09 ships, this directory holds only this README.
